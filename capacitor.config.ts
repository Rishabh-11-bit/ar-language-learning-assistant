import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arvr.languagelearning',
  appName: 'AR Language Learning Assistant',
  webDir: 'www'
};

export default config;
